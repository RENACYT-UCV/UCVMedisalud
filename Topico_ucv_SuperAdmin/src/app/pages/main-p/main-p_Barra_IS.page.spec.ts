import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MainP_Barra_ISPage } from './main-p_Barra_IS.page';

describe('MainP_Barra_ISPage', () => {
  let component: MainP_Barra_ISPage;
  let fixture: ComponentFixture<MainP_Barra_ISPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MainP_Barra_ISPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
