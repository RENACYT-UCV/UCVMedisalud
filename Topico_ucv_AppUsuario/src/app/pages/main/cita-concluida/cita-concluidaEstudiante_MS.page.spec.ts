import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CitaConcluidaPage } from './cita-concluidaEstudiante_MS.page';


describe('CitaConcluidaPage', () => {
  let component: CitaConcluidaPage;
  let fixture: ComponentFixture<CitaConcluidaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CitaConcluidaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
